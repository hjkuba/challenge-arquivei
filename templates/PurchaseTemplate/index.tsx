import { ReactElement } from 'react';

const PurchaseTemplate = (): ReactElement => {
    return (
        <section>
            <h2>Adquira mais Consultas</h2>
        </section>
    );
};

export default PurchaseTemplate;
